/**
 * Dieses Enum listet sämtliche unterstützten Klassifizierer auf, die bei der Erstellung eines KI-Modells verwendet werden.
 */
export enum Classifier {
    MLP,
    RANDOM_FOREST,
    K_NEIGHOBORS,
    SVM
}